# Intel AX210 WiFi configuration for GPD Win5
# Fix for firmware crashes under high load

# Disable power management (most important fix)
# Prevents firmware crashes during power state transitions
options iwlwifi power_save=0

# Additional stability options (uncomment if still having issues):
# options iwlwifi power_save=0 swcrypto=1 11n_disable=8

